src-rust/Cargo.toml text eol=lf

# Bundled sample G-code: a viewer fixture whose exact bytes the parser checks
# against in tests. `-text` disables line-ending conversion while still letting
# git render readable diffs.
public/samples/*.nc           -text
